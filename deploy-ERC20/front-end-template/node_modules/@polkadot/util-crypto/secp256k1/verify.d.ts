import { HashType } from '@polkadot/util-crypto/secp256k1/types';
/**
 * @name secp256k1Verify
 * @description Verifies the signature of `message`, using the supplied pair
 */
export default function secp256k1Verify(message: Uint8Array | string, signature: Uint8Array | string, address: Uint8Array | string, hashType?: HashType, isExpanded?: boolean): boolean;
