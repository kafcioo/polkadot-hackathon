import { ApiInterfaceRx } from '@polkadot/api/types';
import { AnyFunction } from '@polkadot/types/types';
import { Observable } from 'rxjs';
import * as accounts from '@polkadot/api-derive/accounts';
import * as balances from '@polkadot/api-derive/balances';
import * as chain from '@polkadot/api-derive/chain';
import * as contracts from '@polkadot/api-derive/contracts';
import * as council from '@polkadot/api-derive/council';
import * as democracy from '@polkadot/api-derive/democracy';
import * as elections from '@polkadot/api-derive/elections';
import * as imOnline from '@polkadot/api-derive/imOnline';
import * as parachains from '@polkadot/api-derive/parachains';
import * as session from '@polkadot/api-derive/session';
import * as society from '@polkadot/api-derive/society';
import * as staking from '@polkadot/api-derive/staking';
import * as technicalCommittee from '@polkadot/api-derive/technicalCommittee';
import * as treasury from '@polkadot/api-derive/treasury';
import * as tx from '@polkadot/api-derive/tx';
export * from '@polkadot/api-derive/type';
export declare const derive: {
    accounts: typeof accounts;
    balances: typeof balances;
    chain: typeof chain;
    contracts: typeof contracts;
    council: typeof council;
    democracy: typeof democracy;
    elections: typeof elections;
    imOnline: typeof imOnline;
    parachains: typeof parachains;
    session: typeof session;
    society: typeof society;
    staking: typeof staking;
    technicalCommittee: typeof technicalCommittee;
    treasury: typeof treasury;
    tx: typeof tx;
};
declare type DeriveSection<Section> = {
    [Method in keyof Section]: Section[Method] extends AnyFunction ? ReturnType<Section[Method]> : never;
};
declare type DeriveAllSections<AllSections> = {
    [Section in keyof AllSections]: DeriveSection<AllSections[Section]>;
};
export declare type DeriveCustom = Record<string, Record<string, (instanceId: string, api: ApiInterfaceRx) => (...args: any[]) => Observable<any>>>;
export declare type ExactDerive = DeriveAllSections<typeof derive>;
/** @internal */
export default function decorateDerive(instanceId: string, api: ApiInterfaceRx, custom?: DeriveCustom): ExactDerive;
